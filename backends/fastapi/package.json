{
  "name": "@the-forum/fastapi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 8000",
    "start": "uv run uvicorn app.main:app --port 8000",
    "install": "uv sync",
    "lint": "uv run ruff check app",
    "report": "uv run python -m app.scripts.recommendation_report"
  }
}
